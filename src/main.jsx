import { StrictMode, useEffect, useMemo, useState } from 'react';
import { createRoot } from 'react-dom/client';
import { ChevronDown, CupSoda, House, MapPin, Pizza, ReceiptText, Search, ShoppingBag, Sparkles, Sandwich, UserRound } from 'lucide-react';
import './styles.css';

const locations = ['Kalaiya', 'Birgunj', 'Kathmandu', 'Pokhara', 'Bharatpur', 'Biratnagar', 'Butwal', 'Janakpur', 'Hetauda', 'Dharan'];
const categories = [['pizza', 'Pizza', <Pizza size={27} strokeWidth={2.2} aria-label="Pizza icon" />], ['burger', 'Burger', <Sandwich size={27} strokeWidth={2.2} aria-label="Burger icon" />], ['drinks', 'Cold Drinks', <CupSoda size={27} strokeWidth={2.2} aria-label="Cold drinks icon" />]];
const restaurants = [
  { id: 'firestone-pizza', name: 'Firestone Pizza', area: 'Boudha, Kathmandu', cuisine: 'Pizza · Italian', eta: '35–45 min', rating: '4.6', delivery: 69, image: 'https://images.unsplash.com/photo-1528137871618-79d2761e3fd5?w=900&q=85', featured: true },
  { id: 'kathmandu-burger-co', name: 'Kathmandu Burger Co.', area: 'Patan, Lalitpur', cuisine: 'Burgers · Grill', eta: '20–30 min', rating: '4.6', delivery: 49, image: 'https://images.unsplash.com/photo-1553979459-d2229ba7433b?w=900&q=85', featured: true },
  { id: 'rooftop-refresh', name: 'Rooftop Refresh', area: 'Lakeside, Pokhara', cuisine: 'Cold Drinks · Cafe', eta: '15–25 min', rating: '4.7', delivery: 39, image: 'https://images.unsplash.com/photo-1544145945-f90425340c7e?w=900&q=85', featured: true }
];
const item = (id, restaurantId, name, category, price, tag, description, image, addons = []) => ({ id, restaurantId, name, category, price, tag, description, image, addons, isVeg: isVegName(name, category) });
const isVegName = (name, category) => category === 'drinks' || (category === 'burger' ? /(paneer|mushroom|vegan|bean)/i.test(name) : !/(chicken|pepperoni|buff|prosciutto|salami|lamb|beef|teriyaki|bbq|diavola)/i.test(name));
const products = [
  item('spicy-burger-combo', 'kathmandu-burger-co', 'Spicy Burger Combo', 'burger', 599, 'Limited Time Offer', 'Crispy spicy chicken burger with golden fries and a chilled cola.', 'https://images.pexels.com/photos/1639562/pexels-photo-1639562.jpeg?auto=compress&cs=tinysrgb&w=900', [{ name: 'Extra Fries', price: 90 }]),
  item('margherita', 'firestone-pizza', 'Margherita Pizza', 'pizza', 649, 'Classic', 'Stone-baked tomato, mozzarella, basil and olive oil.', 'https://images.unsplash.com/photo-1574071318508-1cdbab80d002?w=900&q=85', [{ name: 'Extra Cheese', price: 100 }]),
  item('pepperoni-pizza', 'firestone-pizza', 'Pepperoni Pizza', 'pizza', 749, 'Best Seller', 'Crisp base layered with pepperoni, mozzarella and oregano.', 'https://images.unsplash.com/photo-1628840042765-356cda07504e?w=900&q=85'),
  item('tandoori-pizza', 'firestone-pizza', 'Tandoori Chicken Pizza', 'pizza', 799, 'Smoky', 'Tandoori chicken, peppers, onion and mozzarella with mint drizzle.', 'https://images.pexels.com/photos/1435907/pexels-photo-1435907.jpeg?auto=compress&cs=tinysrgb&w=900'),
  item('paneer-pizza', 'firestone-pizza', 'Paneer Tikka Pizza', 'pizza', 729, 'Local Favourite', 'Tikka paneer, roasted peppers, onion and coriander on a cheesy base.', 'https://images.unsplash.com/photo-1594007654729-407eedc4be65?w=900&q=85'),
  item('mushroom-pizza', 'firestone-pizza', 'Mushroom Pizza', 'pizza', 699, 'Fresh', 'Garlic mushrooms, herbs, mozzarella and cracked black pepper.', 'https://images.unsplash.com/photo-1565299624946-b28f40a0ae38?w=900&q=85'),
  item('four-cheese-pizza', 'firestone-pizza', 'Four Cheese Pizza', 'pizza', 849, 'Cheesy', 'Mozzarella, cheddar, parmesan and blue cheese with chilli honey.', 'https://images.pexels.com/photos/825661/pexels-photo-825661.jpeg?auto=compress&cs=tinysrgb&w=900'),
  item('bbq-chicken-pizza', 'firestone-pizza', 'BBQ Chicken Pizza', 'pizza', 779, 'Smoky', 'Barbecue chicken, red onion, sweet corn and smoky sauce.', 'https://images.unsplash.com/photo-1571407970349-bc81e7e96d47?w=900&q=85'),
  item('spinach-pizza', 'firestone-pizza', 'Spinach Corn Pizza', 'pizza', 679, 'Value', 'Creamy spinach, sweet corn, onion and mozzarella.', 'https://images.pexels.com/photos/315755/pexels-photo-315755.jpeg?auto=compress&cs=tinysrgb&w=900'),
  item('buff-pizza', 'firestone-pizza', 'Buff Chilli Pizza', 'pizza', 819, 'Nepal Special', 'Spiced buff, green chilli, onion and smoked cheese.', 'https://images.unsplash.com/photo-1579751626657-72bc17010498?w=900&q=85'),
  item('garlic-pizza', 'firestone-pizza', 'Roasted Garlic Pizza', 'pizza', 599, 'Light Bite', 'Roasted garlic, herbs, tomato and mozzarella.', 'https://images.unsplash.com/photo-1513104890138-7c749659a591?w=900&q=85'),
  item('prosciutto-pizza', 'firestone-pizza', 'Prosciutto Pizza', 'pizza', 899, 'Premium', 'Thin-crust pizza with prosciutto, rocket and parmesan.', 'https://images.unsplash.com/photo-1593560708920-61dd98c46a4e?w=900&q=85'),
  item('pesto-pizza', 'firestone-pizza', 'Pesto Tomato Pizza', 'pizza', 719, 'Green Fresh', 'Basil pesto, cherry tomatoes, mozzarella and toasted pine nuts.', 'https://images.unsplash.com/photo-1583778176476-4a8b02a64c01?w=900&q=85'),
  item('hawaiian-pizza', 'firestone-pizza', 'Pineapple Chicken Pizza', 'pizza', 769, 'Sweet & Savoury', 'Roasted chicken, pineapple, mozzarella and chilli flakes.', 'https://images.pexels.com/photos/1566837/pexels-photo-1566837.jpeg?auto=compress&cs=tinysrgb&w=900'),
  item('diavola-pizza', 'firestone-pizza', 'Diavola Pizza', 'pizza', 829, 'Spicy', 'Spicy salami, chilli, tomato and mozzarella on a blistered crust.', 'https://images.pexels.com/photos/1146760/pexels-photo-1146760.jpeg?auto=compress&cs=tinysrgb&w=900'),
  item('corn-pizza', 'firestone-pizza', 'Sweet Corn Pizza', 'pizza', 629, 'Value', 'Sweet corn, peppers, onion and mozzarella with herb oil.', 'https://images.pexels.com/photos/1662130/pexels-photo-1662130.jpeg?auto=compress&cs=tinysrgb&w=900'),
  item('nepalese-pizza', 'firestone-pizza', 'Kathmandu Chilli Pizza', 'pizza', 789, 'DOWNTOWN Special', 'Green chilli, tomato relish, chicken and fresh local herbs.', 'https://images.pexels.com/photos/708587/pexels-photo-708587.jpeg?auto=compress&cs=tinysrgb&w=900'),
  item('classic-burger', 'kathmandu-burger-co', 'Classic Burger', 'burger', 329, 'Classic', 'Grilled beef patty, lettuce, tomato, onion and house sauce.', 'https://images.unsplash.com/photo-1568901346375-23c9450c58cd?w=900&q=85'),
  item('cheese-burger', 'kathmandu-burger-co', 'Classic Cheese Burger', 'burger', 399, 'Best Seller', 'Grilled beef patty, cheddar, lettuce, tomato and house sauce.', 'https://images.unsplash.com/photo-1550547660-d9450f859349?w=900&q=85'),
  item('spicy-chicken-burger', 'kathmandu-burger-co', 'Spicy Chicken Burger', 'burger', 429, 'Spicy', 'Crispy chicken, jalapeño, slaw and smoky chilli mayo.', 'https://images.unsplash.com/photo-1594212699903-ec8a3eca50f5?w=900&q=85'),
  item('double-burger', 'kathmandu-burger-co', 'Double Stack Burger', 'burger', 549, 'Best Seller', 'Two grilled patties, double cheese, pickles and burger sauce.', 'https://images.unsplash.com/photo-1586190848861-99aa4a171e90?w=900&q=85'),
  item('buff-burger', 'kathmandu-burger-co', 'Buff Burger', 'burger', 379, 'Local Favourite', 'Spiced buff patty, tomato relish, greens and cheddar.', 'https://images.unsplash.com/photo-1596662951482-0c4ba74a6df6?w=900&q=85'),
  item('paneer-burger', 'kathmandu-burger-co', 'Paneer Crunch Burger', 'burger', 399, 'Veg Favourite', 'Crispy paneer, lettuce, roasted peppers and mint chutney.', 'https://images.unsplash.com/photo-1520072959219-c595dc870360?w=900&q=85'),
  item('mushroom-burger', 'kathmandu-burger-co', 'Wild Mushroom Burger', 'burger', 449, 'Fresh', 'Garlic mushroom patty, caramelised onion and Swiss cheese.', 'https://images.unsplash.com/photo-1549611016-3a70d82b5040?w=900&q=85'),
  item('bbq-burger', 'kathmandu-burger-co', 'BBQ Smokehouse Burger', 'burger', 499, 'Smoky', 'Grilled beef, crispy onion, barbecue glaze and smoked cheddar.', 'https://images.unsplash.com/photo-1551782450-a2132b4ba21d?w=900&q=85'),
  item('crispy-burger', 'kathmandu-burger-co', 'Crispy Chicken Burger', 'burger', 419, 'Crispy', 'Golden chicken fillet, cabbage slaw and lemon pepper mayo.', 'https://images.unsplash.com/photo-1571091718767-18b5b1457add?w=900&q=85'),
  item('chilli-burger', 'kathmandu-burger-co', 'Chilli Cheese Burger', 'burger', 469, 'Hot', 'Beef patty, green chilli relish, melted cheese and pickles.', 'https://images.unsplash.com/photo-1561758033-d89a9ad46330?w=900&q=85'),
  item('avocado-burger', 'kathmandu-burger-co', 'Avocado Chicken Burger', 'burger', 529, 'Premium', 'Grilled chicken, avocado, greens and coriander lime sauce.', 'https://images.unsplash.com/photo-1550317138-10000687a72b?w=900&q=85'),
  item('lamb-burger', 'kathmandu-burger-co', 'Herb Lamb Burger', 'burger', 579, 'Chef Special', 'Herb-seasoned lamb patty, feta, greens and tomato relish.', 'https://images.unsplash.com/photo-1565299507177-b0ac66763828?w=900&q=85'),
  item('mozzarella-burger', 'kathmandu-burger-co', 'Mozzarella Beef Burger', 'burger', 489, 'Cheesy', 'Beef patty, mozzarella, basil, tomato and garlic aioli.', 'https://images.unsplash.com/photo-1572802419224-296b0aeee0d9?w=900&q=85'),
  item('jalapeno-burger', 'kathmandu-burger-co', 'Jalapeño Smash Burger', 'burger', 459, 'Spicy', 'Smash beef patty, jalapeño, cheddar and smoky house sauce.', 'https://images.unsplash.com/photo-1598182198871-d3f4ab4fd181?w=900&q=85'),
  item('teriyaki-burger', 'kathmandu-burger-co', 'Teriyaki Chicken Burger', 'burger', 439, 'New', 'Grilled chicken, teriyaki glaze, sesame slaw and greens.', 'https://images.unsplash.com/photo-1615297928064-24977384d0da?w=900&q=85'),
  item('truffle-burger', 'kathmandu-burger-co', 'Truffle Mushroom Burger', 'burger', 599, 'Premium', 'Mushroom patty, truffle mayo, Swiss cheese and rocket.', 'https://images.unsplash.com/photo-1586816001966-79b736744398?w=900&q=85'),
  item('vegan-burger', 'kathmandu-burger-co', 'Himalayan Bean Burger', 'burger', 389, 'Plant Based', 'Crispy bean patty, greens, tomato and roasted sesame spread.', 'https://images.pexels.com/photos/3616956/pexels-photo-3616956.jpeg?auto=compress&cs=tinysrgb&w=900'),
  item('coke', 'rooftop-refresh', 'Coca-Cola', 'drinks', 120, 'Classic', 'Chilled Coca-Cola served cold for the perfect burger pairing.', 'https://images.unsplash.com/photo-1622483767028-3f66f32aef97?w=900&q=85'),
  item('sprite', 'rooftop-refresh', 'Sprite', 'drinks', 120, 'Refreshing', 'Crisp lemon-lime soda served chilled.', 'https://images.unsplash.com/photo-1625772299848-391b6a87d7b3?w=900&q=85'),
  item('fanta', 'rooftop-refresh', 'Fanta Orange', 'drinks', 120, 'Fruity', 'Bright, bubbly orange soda served over ice.', 'https://images.unsplash.com/photo-1624517452488-04869289c4ca?w=900&q=85'),
  item('pepsi', 'rooftop-refresh', 'Pepsi', 'drinks', 120, 'Classic', 'Chilled cola with a bold, refreshing taste.', 'https://images.unsplash.com/photo-1629203851122-3726ecdf080e?w=900&q=85'),
  item('iced-tea', 'rooftop-refresh', 'Lemon Iced Tea', 'drinks', 180, 'Fresh', 'Brewed black tea, lemon and ice with a light sweetness.', 'https://images.unsplash.com/photo-1499638673689-79a0b5115d87?w=900&q=85'),
  item('peach-tea', 'rooftop-refresh', 'Peach Iced Tea', 'drinks', 190, 'Fruity', 'Cold brewed tea with ripe peach and a citrus finish.', 'https://images.unsplash.com/photo-1556679343-c7306c1976bc?w=900&q=85'),
  item('sparkling-water', 'rooftop-refresh', 'Sparkling Water', 'drinks', 100, 'Light', 'Cold sparkling mineral water with a clean finish.', 'https://images.unsplash.com/photo-1523362628745-0c100150b504?w=900&q=85'),
  item('lemon-soda', 'rooftop-refresh', 'Fresh Lemon Soda', 'drinks', 160, 'Local Favourite', 'Fresh lemon, soda and mint served over plenty of ice.', 'https://images.unsplash.com/photo-1621263764928-df1444c5e859?w=900&q=85'),
  item('lime-soda', 'rooftop-refresh', 'Mint Lime Soda', 'drinks', 170, 'Refreshing', 'Crushed lime, mint, soda and a touch of Himalayan salt.', 'https://images.unsplash.com/photo-1544145945-f90425340c7e?w=900&q=85'),
  item('blueberry-soda', 'rooftop-refresh', 'Blueberry Fizz', 'drinks', 220, 'Fruity', 'Blueberry syrup, lemon and sparkling soda over ice.', 'https://images.unsplash.com/photo-1546171753-97d7676e4602?w=900&q=85'),
  item('orange-juice', 'rooftop-refresh', 'Fresh Orange Juice', 'drinks', 220, 'Fresh', 'Pressed orange juice served chilled without added water.', 'https://images.unsplash.com/photo-1613478223719-2ab802602423?w=900&q=85'),
  item('apple-juice', 'rooftop-refresh', 'Apple Juice', 'drinks', 190, 'Fresh', 'Chilled, crisp apple juice with natural fruit sweetness.', 'https://images.unsplash.com/photo-1560526860-1f0e56f80788?w=900&q=85'),
  item('mango-shake', 'rooftop-refresh', 'Mango Shake', 'drinks', 260, 'Seasonal', 'Thick mango shake blended with chilled milk.', 'https://images.unsplash.com/photo-1601924582971-f47e19adfcf0?w=900&q=85'),
  item('chocolate-shake', 'rooftop-refresh', 'Chocolate Shake', 'drinks', 280, 'Rich', 'Creamy chocolate shake topped with cocoa and whipped cream.', 'https://images.unsplash.com/photo-1572490122747-3968b75cc699?w=900&q=85'),
  item('vanilla-shake', 'rooftop-refresh', 'Vanilla Shake', 'drinks', 260, 'Classic', 'Smooth vanilla milkshake with a soft, creamy finish.', 'https://images.unsplash.com/photo-1553787434-dd936759214f?w=900&q=85'),
  item('strawberry-shake', 'rooftop-refresh', 'Strawberry Shake', 'drinks', 280, 'Fruity', 'Fresh strawberry shake blended cold and creamy.', 'https://images.unsplash.com/photo-1553530666-ba11a7da3888?w=900&q=85'),
  item('cold-coffee', 'rooftop-refresh', 'Cold Coffee', 'drinks', 240, 'Cafe Favourite', 'Cold brewed coffee, milk and ice with a smooth foam top.', 'https://images.unsplash.com/photo-1461023058943-07fcbe16d735?w=900&q=85')
];
const money = (value) => `Rs. ${Math.round(value).toLocaleString('en-IN')}`;
const load = (key, fallback) => { try { return JSON.parse(localStorage.getItem(key)) ?? fallback; } catch { return fallback; } };
const save = (key, value) => localStorage.setItem(key, JSON.stringify(value));
const defaultUser = { name: 'Aarav Shrestha', phone: '+977 9800000000', email: 'aarav@example.com', photo: '', notifications: true, settings: { darkMode: true, language: 'English' } };
const defaultAddresses = [{ id: 'home', label: 'Home', address: 'Lalitpur, Bagmati Province', city: 'Lalitpur', note: 'Near Patan Durbar Square', isDefault: true }];

function App() {
  const [screen, setScreen] = useState('home');
  const [query, setQuery] = useState('');
  const [category, setCategory] = useState('all');
  const [selected, setSelected] = useState(null);
  const [cart, setCart] = useState(() => load('downtown-cart', []));
  const [favorites, setFavorites] = useState(() => load('downtown-favorites', []));
  const [orders, setOrders] = useState(() => load('downtown-orders', []));
  const [user, setUser] = useState(() => load('downtown-user', null));
  const [addresses, setAddresses] = useState(() => load('downtown-addresses', defaultAddresses));
    const [location, setLocation] = useState(() => { const savedLocation = load('downtown-location', null); return locations.includes(savedLocation) ? savedLocation : 'Kalaiya'; });
  const [serviceMode, setServiceMode] = useState(() => load('downtown-service-mode', 'take-away'));
  const [tableNumber, setTableNumber] = useState(() => load('downtown-table-number', ''));
  const [dietaryFilter, setDietaryFilter] = useState('all');
  const [toast, setToast] = useState('');
  const [overlay, setOverlay] = useState(null);
  const [coupon, setCoupon] = useState('');
  const [couponApplied, setCouponApplied] = useState(false);

  useEffect(() => save('downtown-cart', cart), [cart]); useEffect(() => save('downtown-favorites', favorites), [favorites]); useEffect(() => save('downtown-orders', orders), [orders]); useEffect(() => save('downtown-user', user), [user]); useEffect(() => save('downtown-addresses', addresses), [addresses]); useEffect(() => save('downtown-location', location), [location]); useEffect(() => save('downtown-service-mode', serviceMode), [serviceMode]); useEffect(() => save('downtown-table-number', tableNumber), [tableNumber]);
  useEffect(() => { if (!toast) return undefined; const timer = setTimeout(() => setToast(''), 2400); return () => clearTimeout(timer); }, [toast]);
  useEffect(() => { if (!location && !overlay) setOverlay('location'); }, [location, overlay]);
  const notify = (text) => setToast(text);
  const cartCount = cart.reduce((sum, item) => sum + item.quantity, 0);
  const subtotal = cart.reduce((sum, item) => sum + item.price * item.quantity, 0);
  const delivery = cart.length ? (restaurants.find((r) => r.id === cart[0].restaurantId)?.delivery ?? 59) : 0;
  const service = cart.length ? Math.round(subtotal * 0.03) : 0;
  const discount = couponApplied ? 100 : 0;
  const total = Math.max(0, subtotal + delivery + service - discount);
  const visibleProducts = useMemo(() => { const text = query.trim().toLowerCase(); return products.filter((p) => (!text ? category === 'all' || p.category === category : `${p.name} ${p.category} ${p.description}`.toLowerCase().includes(text))).filter((p) => dietaryFilter === 'all' || (dietaryFilter === 'veg' ? p.isVeg : !p.isVeg)); }, [query, category, dietaryFilter]);
  const go = (next) => { setOverlay(null); setScreen(next); };
  const openProduct = (product) => { setSelected(product); setScreen('details'); };
  const toggleFavorite = (product) => setFavorites((current) => current.includes(product.id) ? current.filter((id) => id !== product.id) : [...current, product.id]);
  const addToCart = (product, quantity = 1, addon = null) => { setCart((current) => { const key = `${product.id}-${addon?.name ?? 'none'}`; const existing = current.find((item) => item.key === key); return existing ? current.map((item) => item.key === key ? { ...item, quantity: item.quantity + quantity } : item) : [...current, { ...product, key, quantity, addon }]; }); notify(`${product.name} added to cart`); };
  const updateQuantity = (key, delta) => setCart((current) => current.map((item) => item.key === key ? { ...item, quantity: Math.max(0, item.quantity + delta) } : item).filter((item) => item.quantity));
  const defaultAddress = addresses.find((a) => a.isDefault) || addresses[0];
  const activeLocation = typeof location === 'string' && location.trim() ? location : 'Kalaiya';
  const orderModeLabel = serviceMode === 'dine-in' ? `Dine In · Table ${tableNumber || 'Unassigned'}` : 'Take Away';
  return <div className="app-shell ambient-background"><div className="ambient-orb orb-one" /><div className="ambient-orb orb-two" /><main className="phone-canvas">
    <header className="app-header"><div className="status-bar"><span className="brand-mini">DOWNTOWN</span><span className="header-time">Sunday · 9:41</span></div><div className="location-bar"><button className="location-row" onClick={() => setOverlay('location')} aria-label={`Change delivery location from ${activeLocation}`}><MapPin size={18} strokeWidth={2.2} /><span><small>Delivering to</small><strong>{activeLocation}</strong></span><ChevronDown className="location-chevron" size={16} strokeWidth={2.5} /></button><AccountAvatar user={user} orders={orders} addresses={addresses} onOpenProfile={() => setOverlay('edit-profile')} onViewOrder={() => go('orders')} onAddresses={() => setOverlay('addresses')} onOrders={() => go('orders')} onLogout={() => { setUser(null); notify('Logged out'); }} onDelete={() => { setUser(null); setAddresses([]); notify('Account deleted'); }} /></div></header>
    {screen === 'home' && <><ServicePanel mode={serviceMode} setMode={setServiceMode} tableNumber={tableNumber} setTableNumber={setTableNumber} showTable={false} /><DietaryFilter value={dietaryFilter} onChange={setDietaryFilter} /><Home {...{query, setQuery, category, setCategory, visibleProducts, favorites, toggleFavorite, openProduct, addToCart, location: activeLocation, setOverlay, go, orders, user, serviceMode, setServiceMode, dietaryFilter, setDietaryFilter}} /></>}
    {screen === 'home' && query.trim() && <div className="home-search-results-overlay"><InlineSearchResults query={query} products={visibleProducts} favorites={favorites} toggleFavorite={toggleFavorite} openProduct={openProduct} addToCart={addToCart} /></div>}
    {screen === 'search' && <><DietaryFilter value={dietaryFilter} onChange={setDietaryFilter} /><SearchScreen {...{query, setQuery, category, setCategory, visibleProducts, favorites, toggleFavorite, openProduct, addToCart, dietaryFilter, setDietaryFilter}} /></>}
    {screen === 'details' && selected && <Details product={selected} favorite={favorites.includes(selected.id)} onBack={() => go('home')} onFavorite={() => toggleFavorite(selected)} onAdd={addToCart} />}
    {screen === 'orders' && <Orders orders={orders} onHome={() => go('home')} onReorder={(items) => { items.forEach((item) => addToCart(products.find((p) => p.id === item.id) || item, item.quantity)); go('cart'); }} />}
    {screen === 'cart' && <><ServicePanel mode={serviceMode} setMode={setServiceMode} tableNumber={tableNumber} setTableNumber={setTableNumber} /><LegacyCart cart={cart} subtotal={subtotal} delivery={delivery} service={service} discount={discount} total={total} updateQuantity={updateQuantity} onHome={() => go('home')} onCheckout={() => user ? go('checkout') : setOverlay('auth')} onClear={() => setCart([])} coupon={coupon} setCoupon={setCoupon} couponApplied={couponApplied} applyCoupon={() => { if (coupon.trim().toUpperCase() === 'DOWNTOWN100') { setCouponApplied(true); notify('Rs. 100 coupon applied'); } else notify('Try code DOWNTOWN100'); }} serviceMode={serviceMode} setServiceMode={setServiceMode} tableNumber={tableNumber} setTableNumber={setTableNumber} /></>}
    {screen === 'checkout' && <><ServicePanel mode={serviceMode} setMode={setServiceMode} tableNumber={tableNumber} setTableNumber={setTableNumber} /><Checkout {...{cart, subtotal, delivery, service, discount, total, addresses, defaultAddress, user, location: activeLocation, couponApplied, serviceMode, tableNumber, setTableNumber, onBack: () => go('cart'), onPlace: (order) => { setOrders((current) => [{ ...order, city: `${order.city} · ${orderModeLabel}` }, ...current]); setCart([]); go('confirmation'); } }} /></>}
    {screen === 'confirmation' && <Confirmation onOrders={() => go('orders')} onHome={() => go('home')} />}
    {screen === 'profile' && <Profile user={user} addresses={addresses} orders={orders} onAuth={() => setOverlay('auth')} onEdit={() => setOverlay('edit-profile')} onAddresses={() => setOverlay('addresses')} onLogout={() => { setUser(null); notify('Logged out'); }} onDelete={() => { setUser(null); setAddresses([]); notify('Account deleted'); }} onOrders={() => go('orders')} />}
    {screen !== 'details' && screen !== 'checkout' && screen !== 'confirmation' && <Nav screen={screen} cartCount={cartCount} go={go} />}
    {toast && <div className="toast">● &nbsp;{toast}</div>}
    {overlay === 'auth' && <Auth onClose={() => setOverlay(null)} onSuccess={(account) => { setUser(account); setOverlay(null); notify('Welcome to DOWNTOWN'); }} />}
    {overlay === 'location' && <LocationModal location={activeLocation} addresses={addresses} onClose={() => setOverlay(null)} onSelect={(next) => { setLocation(next); setOverlay(null); notify(`Delivering to ${next}`); }} onSave={(address) => { setAddresses((current) => [...current, { ...address, id: `address-${Date.now()}`, isDefault: current.length === 0 }]); setLocation(address.city); setOverlay(null); notify('Address saved'); }} />}
    {overlay === 'addresses' && <AddressManager addresses={addresses} onClose={() => setOverlay(null)} onUpdate={setAddresses} />}
    {overlay === 'edit-profile' && <EditProfile user={user} onClose={() => setOverlay(null)} onSave={(next) => { setUser(next); setOverlay(null); notify('Profile updated'); }} />}
    {overlay === 'filter' && <Filter onClose={() => setOverlay(null)} />}
  </main></div>;
}
function AccountAvatar({ user, orders, addresses, onOpenProfile, onViewOrder, onAddresses, onOrders, onLogout, onDelete }) {
  const [open, setOpen] = useState(false);
  const [settingsOpen, setSettingsOpen] = useState(false);
  return <div className="account-avatar-wrap">
    <button className="avatar" onClick={() => setOpen((o) => !o)} aria-label="Account menu">{user?.photo ? <img src={user.photo} alt="Profile" /> : user?.name?.[0] || <UserRound size={19} strokeWidth={2.2} />}</button>
    {open && <div className="account-popover">
      <div className="account-popover-header"><div className="account-popover-avatar">{user?.photo ? <img src={user.photo} alt="" /> : user?.name?.[0] || <UserRound size={18} strokeWidth={2.2} />}</div><div><b>{user?.name || 'Guest'}</b><small>{user?.email || 'Not logged in'}</small></div></div>
      {settingsOpen ? <>
        <div className="account-settings-list">
          <div className="account-settings-row"><span>Notifications</span><i className={user?.notifications ? 'mini-toggle on' : 'mini-toggle'} /></div>
          <div className="account-settings-row"><span>Language</span><small>English</small></div>
          <div className="account-settings-row"><span>Dark mode</span><i className="mini-toggle on" /></div>
          <button className="account-menu-item" onClick={() => { window.open('mailto:support@downtown.com'); }}>Help & Support</button>
          <button className="account-menu-item danger" onClick={onDelete}>Delete account</button>
        </div>
        <button className="account-popover-back" onClick={() => setSettingsOpen(false)}>‹ Back</button>
      </> : <>
        <div className="account-popover-orders"><p className="account-popover-label">Recent orders</p>{orders.length ? orders.slice(0, 2).map((o) => <button key={o.id} className="account-order-row" onClick={() => { onViewOrder(o.id); setOpen(false); }}><span>{o.number || o.id}</span><small>{o.status}</small></button>) : <p className="account-popover-empty">No orders yet</p>}</div>
        <div className="account-menu-list">
          <button className="account-menu-item" onClick={() => { onOpenProfile(); setOpen(false); }}>Edit profile</button>
          <button className="account-menu-item" onClick={() => { onAddresses(); setOpen(false); }}>Saved addresses <small>{addresses?.length || 0}</small></button>
          <button className="account-menu-item" onClick={() => { onOrders(); setOpen(false); }}>Order history</button>
          <button className="account-menu-item" onClick={() => setSettingsOpen(true)}>Settings</button>
          {user && <button className="account-menu-item danger" onClick={() => { onLogout(); setOpen(false); }}>Log out</button>}
        </div>
      </>}
    </div>}
  </div>;
}
function Home({ query, setQuery, category, setCategory, visibleProducts, favorites, toggleFavorite, openProduct, addToCart, location, setOverlay, go, orders, user }) { return <section className="screen home-screen"><div className="home-top"><div><p className="eyebrow">Delivering to</p><button className="place-button" onClick={() => setOverlay('location')}>⌖ {location} <span>⌄</span></button></div></div><div className="greeting"><p>Namaste, <b>{user?.name?.split(' ')[0] || 'there'}</b> <span>👋</span></p><h1>Good food.<br /><em>Great Nepal.</em></h1></div><div className="search-row"><label className="search-box"><span>⌕</span><input value={query} onChange={(e) => setQuery(e.target.value)} placeholder="Search restaurants or dishes" aria-label="Search" /></label><button className="icon-button" onClick={() => setOverlay('filter')} aria-label="Open filters">☷</button></div><Promo onClick={() => openProduct(products[0])} /><div className="section-heading"><h2>Explore categories</h2><button onClick={() => setCategory('all')}>View all</button></div><div className="category-row">{categories.map(([id, label, icon]) => <button key={id} className={`category ${category === id ? 'active' : ''}`} onClick={() => setCategory(id)}><strong>{icon}</strong><span>{label}</span></button>)}</div><div className="section-heading"><h2>Popular near you</h2><button onClick={() => go('search')}>See all</button></div><div className="restaurant-list">{restaurants.map((restaurant) => <RestaurantCard key={restaurant.id} restaurant={restaurant} />)}</div><div className="section-heading"><h2>Recommended for you</h2><button onClick={() => go('search')}>View all</button></div><div className="product-scroller">{visibleProducts.slice(0, 5).map((product) => <LegacyProductCardOriginal key={product.id} product={product} favorite={favorites.includes(product.id)} onFavorite={() => toggleFavorite(product)} onOpen={() => openProduct(product)} onAdd={() => addToCart(product)} />)}</div>{orders[0] && <button className="recent-order" onClick={() => go('orders')}>↻ &nbsp; Reorder from your last meal <b>{orders[0].number}</b></button>}</section>; }
function InlineSearchResults({ query, products: items, favorites, toggleFavorite, openProduct, addToCart }) { return <div className="inline-search-results"><div className="section-heading"><h2>Results for “{query}”</h2><span>{items.length} found</span></div>{items.length ? <div className="product-scroller">{items.map((product) => <LegacyProductCardOriginal key={product.id} product={product} favorite={favorites.includes(product.id)} onFavorite={() => toggleFavorite(product)} onOpen={() => openProduct(product)} onAdd={() => addToCart(product)} />)}</div> : <Empty title="No meals found" text="Try pizza, burger, or drink." />}</div>; }
function SearchScreen({ query, setQuery, category, setCategory, visibleProducts, favorites, toggleFavorite, openProduct, addToCart }) { return <section className="screen collection-screen"><div className="page-heading"><p>DOWNTOWN discovery</p><h2>Find your next meal</h2><label className="search-box"><span>⌕</span><input autoFocus value={query} onChange={(e) => setQuery(e.target.value)} placeholder="Search Kathmandu favourites" aria-label="Search meals" /></label></div><div className="category-row">{categories.map(([id, label, icon]) => <button key={id} className={`category ${category === id ? 'active' : ''}`} onClick={() => setCategory(id)}><strong>{icon}</strong><span>{label}</span></button>)}</div><div className="collection-grid">{visibleProducts.map((product) => <LegacyProductCardOriginal key={product.id} product={product} favorite={favorites.includes(product.id)} onFavorite={() => toggleFavorite(product)} onOpen={() => openProduct(product)} onAdd={() => addToCart(product)} />)}</div>{!visibleProducts.length && <Empty title="No meals found" text="Try another dish or category." />}</section>; }
function Promo({ onClick }) { return <button className="promo-banner" onClick={onClick}><div className="promo-copy"><small className="promo-badge"><Sparkles size={12} strokeWidth={2.5} /> Limited Time Offer</small><h2>Spicy Burger<br /><em>Combo</em></h2><span className="order-pill">Order Now</span></div><b className="discount-badge">20%<small>OFF</small></b><img className="promo-photo" src={products.find((product) => product.id === 'spicy-burger-combo')?.image} alt="Spicy burger combo" /></button>; }
function RestaurantCard({ restaurant }) { return <article className="restaurant-card"><img src={restaurant.image} alt={restaurant.name} /><div><div className="restaurant-meta"><span>★ {restaurant.rating}</span><span>{restaurant.eta}</span></div><h3>{restaurant.name}</h3><p>{restaurant.cuisine} · {restaurant.area}</p><small>Delivery from {money(restaurant.delivery)}</small></div></article>; }
function DietaryIcon({ isVeg }) { return <span className={`dietary-icon ${isVeg ? 'veg' : 'non-veg'}`} aria-label={isVeg ? 'Vegetarian' : 'Non-vegetarian'}><i /></span>; }
function ServiceToggle({ value, onChange }) { return <div className="service-toggle" role="group" aria-label="Order type"><button className={value === 'take-away' ? 'active' : ''} onClick={() => onChange('take-away')}>Take Away</button><button className={value === 'dine-in' ? 'active' : ''} onClick={() => onChange('dine-in')}>Dine In</button></div>; }
function DietaryFilter({ value, onChange }) { return <div className="dietary-filter" role="group" aria-label="Dietary filter"><span>Menu</span><button className={value === 'all' ? 'active' : ''} onClick={() => onChange('all')}>All</button><button className={value === 'veg' ? 'active veg' : ''} onClick={() => onChange('veg')}>Veg</button><button className={value === 'non-veg' ? 'active non-veg' : ''} onClick={() => onChange('non-veg')}>Non-Veg</button></div>; }
function ServicePanel({ mode, setMode, tableNumber, setTableNumber, showTable = true }) { return <div className="service-panel"><ServiceToggle value={mode} onChange={setMode} />{showTable && mode === 'dine-in' && <label className="table-number"><span>Table Number</span><input value={tableNumber} onChange={(e) => setTableNumber(e.target.value)} inputMode="numeric" placeholder="Optional" aria-label="Table Number" /></label>}</div>; }
function ProductCard({ product, favorite, onFavorite, onOpen, onAdd }) { return <article className="product-card" onClick={onOpen}><button className={`heart-button ${favorite ? 'favorite' : ''}`} aria-label={`${favorite ? 'Remove' : 'Add'} ${product.name} favorite`} onClick={(e) => { e.stopPropagation(); onFavorite(); }}>♥</button><div className="product-image"><img src={product.image} alt={product.name} /><span>{product.tag}</span></div><div className="product-name-row"><DietaryIcon isVeg={product.isVeg} /><h3>{product.name}</h3></div><p>{restaurants.find((r) => r.id === product.restaurantId)?.name}</p><div className="card-footer"><strong>{money(product.price)}</strong><button className="add-button" aria-label={`Add ${product.name} to cart`} onClick={(e) => { e.stopPropagation(); onAdd(); }}>+</button></div></article>; }
function LegacyProductCard({ ...props }) { return <ProductCard {...props} />; }
function LegacyProductCardOriginal({ product, favorite, onFavorite, onOpen, onAdd }) { return <article className="product-card" onClick={onOpen}><button className={`heart-button ${favorite ? 'favorite' : ''}`} aria-label={`${favorite ? 'Remove' : 'Add'} ${product.name} favorite`} onClick={(e) => { e.stopPropagation(); onFavorite(); }}>♥</button><div className="product-image"><img src={product.image} alt={product.name} /><span>{product.tag}</span></div><h3>{product.name}</h3><p>{restaurants.find((r) => r.id === product.restaurantId)?.name}</p><div className="card-footer"><strong>{money(product.price)}</strong><button className="add-button" aria-label={`Add ${product.name} to cart`} onClick={(e) => { e.stopPropagation(); onAdd(); }}>+</button></div></article>; }
function Details({ product, favorite, onBack, onFavorite, onAdd }) { const [quantity, setQuantity] = useState(1); const [addon, setAddon] = useState(null); const itemTotal = (product.price + (addon?.price || 0)) * quantity; return <section className="details-screen screen"><div className="detail-hero"><img src={product.image} alt={product.name} /><div className="detail-actions"><button className="icon-button" onClick={onBack}>‹</button><button className={`icon-button ${favorite ? 'favorite' : ''}`} onClick={onFavorite}>♥</button></div></div><div className="detail-sheet"><span className="detail-badge">✦ {product.tag}</span><div className="detail-title-row"><div><h2>{product.name}</h2><p>★ <b>4.8</b> <span> · {restaurants.find((r) => r.id === product.restaurantId)?.name}</span></p></div><strong>{money(product.price)}</strong></div><p className="description">{product.description}</p>{product.addons.length > 0 && <div className="customize"><h3>Add something extra</h3>{product.addons.map((option) => <button className="toggle-row" key={option.name} onClick={() => setAddon(addon?.name === option.name ? null : option)}><span>{option.name} <small>(+{money(option.price)})</small></span><i className={addon?.name === option.name ? 'toggle on' : 'toggle'}><b /></i></button>)}</div>}<div className="detail-footer"><div className="quantity"><button onClick={() => setQuantity(Math.max(1, quantity - 1))}>−</button><b>{quantity}</b><button onClick={() => setQuantity(quantity + 1)}>+</button></div><button className="primary-action" onClick={() => onAdd(product, quantity, addon)}>🛍 Add to cart <strong>{money(itemTotal)}</strong></button></div></div></section>; }
function Cart({ cart, subtotal, delivery, service, discount, total, updateQuantity, onHome, onCheckout, onClear, coupon, setCoupon, couponApplied, applyCoupon }) { return <section className="screen utility-screen"><PageTitle title="Your cart" onBack={onHome} />{cart.length ? <><p className="cart-context">Delivering to your saved address in Nepal</p><div className="cart-list">{cart.map((item) => <div className={`cart-item ${item.isVeg ? 'veg' : 'non-veg'}`} key={item.key}><img src={item.image} alt="" /><div><h3><DietaryIcon isVeg={item.isVeg} />{item.name}</h3><small>{item.addon?.name || 'Regular'}</small><strong>{money(item.price + (item.addon?.price || 0))}</strong><div className="quantity small"><button onClick={() => updateQuantity(item.key, -1)}>−</button><b>{item.quantity}</b><button onClick={() => updateQuantity(item.key, 1)}>+</button></div></div><button className="remove" onClick={() => updateQuantity(item.key, -item.quantity)}>×</button></div>)}</div><div className="coupon"><input value={coupon} onChange={(e) => setCoupon(e.target.value)} placeholder="Coupon code" aria-label="Coupon code" /><button onClick={applyCoupon}>{couponApplied ? 'Applied' : 'Apply'}</button></div><Summary {...{subtotal, delivery, service, discount, total}} /><button className="secondary-action" onClick={onClear}>Clear cart</button><button className="primary-action full" onClick={onCheckout}>Continue to checkout <strong>{money(total)}</strong></button></> : <Empty title="Your cart is empty" text="Add a local favourite to get started." action="Browse DOWNTOWN" onAction={onHome} />}</section>; }
function LegacyCart({ cart, subtotal, delivery, service, discount, total, updateQuantity, onHome, onCheckout, onClear, coupon, setCoupon, couponApplied, applyCoupon }) { return <Cart {...{ cart, subtotal, delivery, service, discount, total, updateQuantity, onHome, onCheckout, onClear, coupon, setCoupon, couponApplied, applyCoupon }} />; }
function Checkout({ cart, subtotal, delivery, service, discount, total, addresses, defaultAddress, user, location, onBack, onPlace }) { const [form, setForm] = useState({ name: user?.name || '', phone: user?.phone || '', address: defaultAddress?.address || '', payment: 'Cash on Delivery' }); const [error, setError] = useState(''); const [esewaStatus, setEsewaStatus] = useState('idle'); const placeFinalOrder = () => { onPlace({ id: `order-${Date.now()}`, number: `#DW${Math.floor(1000 + Math.random() * 9000)}`, date: new Date().toLocaleDateString('en-NP'), status: 'Order Placed', items: cart, total, address: form.address, city: location, payment: form.payment, timeline: ['Order Placed'] }); }; const submit = (e) => { e.preventDefault(); if (!form.name.trim() || !/^\+977\s?9\d{9}$/.test(form.phone.replace(/-/g, '')) || !form.address.trim()) { setError('Enter your name, a valid Nepal phone (+977 98XXXXXXXX) and delivery address.'); return; } setError(''); if (form.payment === 'eSewa') { setEsewaStatus('processing'); setTimeout(() => { setEsewaStatus('success'); setTimeout(placeFinalOrder, 700); }, 1400); } else { placeFinalOrder(); } }; return <section className="screen utility-screen"><PageTitle title="Checkout" onBack={onBack} /><form className="checkout-form" onSubmit={submit}><div className="checkout-location">⌖ <div><small>Delivering to</small><b>{location}</b><span>{form.address}</span></div></div><label>Full name<input value={form.name} onChange={(e) => setForm({ ...form, name: e.target.value })} placeholder="Aarav Shrestha" /></label><label>Nepal phone number<input type="tel" value={form.phone} onChange={(e) => setForm({ ...form, phone: e.target.value })} placeholder="+977 9800000000" /></label><label>Delivery address<textarea value={form.address} onChange={(e) => setForm({ ...form, address: e.target.value })} placeholder="House, street, neighbourhood" /></label><div className="payment"><h3>Payment method</h3><button type="button" className={`payment-option ${form.payment === 'eSewa' ? 'selected' : ''}`} onClick={() => setForm({ ...form, payment: 'eSewa' })}><span className="payment-logo esewa-logo">e</span><span className="payment-label"><b>eSewa</b><small>Pay securely with your eSewa wallet</small></span><i className={form.payment === 'eSewa' ? 'radio on' : 'radio'} /></button><button type="button" className={`payment-option ${form.payment === 'Cash on Delivery' ? 'selected' : ''}`} onClick={() => setForm({ ...form, payment: 'Cash on Delivery' })}><span className="payment-logo cod-logo">Rs</span><span className="payment-label"><b>Cash on Delivery</b><small>Pay when your order arrives</small></span><i className={form.payment === 'Cash on Delivery' ? 'radio on' : 'radio'} /></button></div>{form.payment === 'eSewa' && esewaStatus !== 'idle' && <div className="esewa-status"><p>{esewaStatus === 'processing' ? '⏳ Redirecting to eSewa...' : '✓ Payment Successful — placing your order'}</p></div>}{error && <p className="form-error">{error}</p>}<Summary {...{subtotal, delivery, service, discount, total}} /><button className="primary-action full" type="submit" disabled={esewaStatus === 'processing'}>{form.payment === 'eSewa' ? (esewaStatus === 'processing' ? 'Processing…' : 'Pay with eSewa') : 'Place order'} <strong>{money(total)}</strong></button></form></section>; }
function Orders({ orders, onHome, onReorder }) { return <section className="screen utility-screen"><PageTitle title="Your orders" onBack={onHome} />{orders.length ? <div className="orders-list">{orders.map((order) => <article className="order-card" key={order.id}><div className="order-top"><span>Order {order.number}</span><strong>{order.status}</strong></div><h3>{order.items.map((item) => `${item.name} ×${item.quantity}`).join(', ')}</h3><small>{order.date} · {order.city}</small><div className="timeline">{['Order Placed', 'Confirmed', 'Preparing', 'Out for Delivery', 'Delivered'].map((state, index) => <span className={index <= ['Order Placed', 'Confirmed', 'Preparing', 'Out for Delivery', 'Delivered'].indexOf(order.status) ? 'done' : ''} key={state}><i />{state}</span>)}</div><b className="order-total">{money(order.total)}</b><button className="secondary-action" onClick={() => onReorder(order.items)}>Reorder</button></article>)}</div> : <Empty title="No orders yet" text="Your DOWNTOWN orders and delivery status will appear here." action="Start ordering" onAction={onHome} />}</section>; }
function Profile({ user, addresses, orders, onAuth, onEdit, onAddresses, onLogout, onDelete, onOrders }) { return <section className="screen utility-screen profile-screen"><div className="profile-hero"><div className="profile-avatar">{user ? (user.photo ? <img src={user.photo} alt="Profile" /> : user.name[0]) : '?'}</div><div><p className="eyebrow">DOWNTOWN account</p><h2>{user?.name || 'Welcome to DOWNTOWN'}</h2><span>{user?.email || 'Log in to manage your account'}</span></div></div>{user ? <><div className="profile-stats"><span><b>{orders.length}</b>Orders</span><span><b>{addresses.length}</b>Addresses</span><span><b>{user.notifications ? 'On' : 'Off'}</b>Alerts</span></div><button className="profile-option" onClick={onEdit}>♙ <span>Edit personal details<small>Phone, email and photo</small></span>›</button><button className="profile-option" onClick={onAddresses}>⌖ <span>Saved addresses<small>{addresses.length} delivery locations</small></span>›</button><button className="profile-option" onClick={onOrders}>▣ <span>Order history<small>Track and reorder meals</small></span>›</button><button className="profile-option" onClick={onEdit}>⚙ <span>Notifications & settings<small>Manage your preferences</small></span>›</button><button className="secondary-action logout" onClick={onLogout}>Log out</button><button className="danger-action" onClick={onDelete}>Delete account</button></> : <><Empty title="Your account, your way" text="Sign up to save addresses, track orders and reorder favourites." action="Log in or sign up" onAction={onAuth} /></>}</section>; }
function Auth({ onClose, onSuccess }) { const [mode, setMode] = useState('login'); const [form, setForm] = useState({ name: '', phone: '', email: '', password: '' }); const [error, setError] = useState(''); const submit = (e) => { e.preventDefault(); if ((mode === 'signup' && !form.name.trim()) || !form.email.includes('@') || form.password.length < 4) { setError('Enter the required details. Password must be at least 4 characters.'); return; } const { password: _password, ...account } = form; onSuccess({ ...defaultUser, ...account }); }; return <div className="modal-backdrop"><form className="auth-modal" onSubmit={submit}><div className="modal-title"><div><p className="eyebrow">Welcome to</p><h2>DOWNTOWN</h2></div><button type="button" onClick={onClose}>×</button></div>{mode === 'signup' && <label>Full name<input value={form.name} onChange={(e) => setForm({ ...form, name: e.target.value })} placeholder="Your name" /></label>}<label>Email<input type="email" value={form.email} onChange={(e) => setForm({ ...form, email: e.target.value })} placeholder="you@example.com" /></label>{mode === 'signup' && <label>Phone<input value={form.phone} onChange={(e) => setForm({ ...form, phone: e.target.value })} placeholder="+977 9800000000" /></label>}<label>Password<input type="password" value={form.password} onChange={(e) => setForm({ ...form, password: e.target.value })} placeholder="••••" /></label>{error && <p className="form-error">{error}</p>}<button className="primary-action full" type="submit">{mode === 'login' ? 'Log in' : 'Create account'}</button><button className="secondary-action" type="button" onClick={() => setMode(mode === 'login' ? 'signup' : 'login')}>{mode === 'login' ? 'New to DOWNTOWN? Sign up' : 'Already have an account? Log in'}</button></form></div>; }
function LocationModal({ location, addresses, onClose, onSelect, onSave }) { const [manual, setManual] = useState(''); const [showForm, setShowForm] = useState(false); const [form, setForm] = useState({ label: 'Other', address: '', city: location === 'Choose location' ? '' : location, note: '' }); const useCurrent = () => { if (!navigator.geolocation) { onClose(); return; } navigator.geolocation.getCurrentPosition(() => onSelect('Current location'), () => onClose()); }; return <div className="modal-backdrop"><div className="location-modal"><div className="modal-title"><div><p className="eyebrow">Delivery location</p><h2>Where should we deliver?</h2></div><button onClick={onClose}>×</button></div><button className="current-location" onClick={useCurrent}>⌖ Use my current location <small>Browser permission required</small></button><label className="location-search">⌕<input value={manual} onChange={(e) => setManual(e.target.value)} placeholder="Search a city or neighbourhood" /></label>{locations.filter((city) => !manual || city.toLowerCase().includes(manual.toLowerCase())).map((city) => <button className={`location-option ${city === location ? 'selected' : ''}`} key={city} onClick={() => onSelect(city)}>⌖ <span>{city}<small>Nepal</small></span>{city === location && '✓'}</button>)}<button className="secondary-action" onClick={() => setShowForm(!showForm)}>+ Enter location manually</button>{showForm && <div className="address-form"><select value={form.label} onChange={(e) => setForm({ ...form, label: e.target.value })}><option>Home</option><option>Work</option><option>Other</option></select><input value={form.address} onChange={(e) => setForm({ ...form, address: e.target.value })} placeholder="Full address" /><input value={form.city} onChange={(e) => setForm({ ...form, city: e.target.value })} placeholder="City" /><button className="primary-action full" onClick={() => form.address && form.city && onSave(form)}>Save address</button></div>}{addresses.length > 0 && <p className="saved-note">{addresses.length} saved address{addresses.length > 1 ? 'es' : ''} available from your profile.</p>}</div></div>; }
function AddressManager({ addresses, onClose, onUpdate }) { const remove = (id) => onUpdate(addresses.filter((a) => a.id !== id)); const edit = (address) => { const next = window.prompt('Update delivery address', address.address); if (next?.trim()) onUpdate(addresses.map((item) => item.id === address.id ? { ...item, address: next.trim() } : item)); }; const makeDefault = (id) => onUpdate(addresses.map((item) => ({ ...item, isDefault: item.id === id }))); return <div className="modal-backdrop"><div className="location-modal"><div className="modal-title"><h2>Saved addresses</h2><button onClick={onClose}>×</button></div>{addresses.map((address) => <div className="saved-address" key={address.id}><div><b>{address.label} {address.isDefault && <small>Default</small>}</b><p>{address.address}, {address.city}</p><div className="address-actions"><button onClick={() => edit(address)}>Edit</button>{!address.isDefault && <button onClick={() => makeDefault(address.id)}>Make default</button>}<button onClick={() => remove(address.id)}>Delete</button></div></div></div>)}<button className="primary-action full" onClick={onClose}>Done</button></div></div>; }
function EditProfile({ user, onClose, onSave }) { const [form, setForm] = useState(user); return <div className="modal-backdrop"><form className="auth-modal" onSubmit={(e) => { e.preventDefault(); onSave(form); }}><div className="modal-title"><h2>Edit profile</h2><button type="button" onClick={onClose}>×</button></div><label>Name<input value={form.name} onChange={(e) => setForm({ ...form, name: e.target.value })} /></label><label>Phone<input value={form.phone} onChange={(e) => setForm({ ...form, phone: e.target.value })} /></label><label>Email<input value={form.email} onChange={(e) => setForm({ ...form, email: e.target.value })} /></label><label>Profile photo URL<input value={form.photo} onChange={(e) => setForm({ ...form, photo: e.target.value })} placeholder="https://..." /></label><label className="check-row"><input type="checkbox" checked={form.notifications} onChange={(e) => setForm({ ...form, notifications: e.target.checked })} /> Receive delivery notifications</label><button className="primary-action full">Save changes</button></form></div>; }
function CartSummary({ subtotal, delivery, service, discount, total }) { return <div className="summary"><p><span>Subtotal</span><b>{money(subtotal)}</b></p><p><span>Delivery fee</span><b>{money(delivery)}</b></p><p><span>Service charge</span><b>{money(service)}</b></p>{discount > 0 && <p><span>Coupon discount</span><b className="discount">−{money(discount)}</b></p>}<p className="total"><span>Total</span><b>{money(total)}</b></p></div>; }
function Summary(props) { return <CartSummary {...props} />; }
function PageTitle({ title, onBack }) { return <div className="page-title"><button className="icon-button" onClick={onBack}>‹</button><h2>{title}</h2><span /></div>; }
function Empty({ title, text, action, onAction }) { return <div className="empty-state"><div>✦</div><h3>{title}</h3><p>{text}</p>{action && <button className="primary-action" onClick={onAction}>{action}</button>}</div>; }
function Confirmation({ onOrders, onHome }) { return <section className="screen confirmation"><div className="success-mark">✓</div><h2>Order placed!</h2><p>Your DOWNTOWN kitchen is getting started. You can track delivery progress from Orders.</p><button className="primary-action full" onClick={onOrders}>Track my order</button><button className="secondary-action" onClick={onHome}>Back to home</button></section>; }
function Nav({ screen, cartCount, go }) { const items = [['home', House, 'Home'], ['search', Search, 'Search'], ['orders', ReceiptText, 'Orders'], ['cart', ShoppingBag, 'Cart'], ['profile', UserRound, 'Profile']]; return <nav className="bottom-nav">{items.map(([id, Icon, label]) => <button key={id} className={screen === id ? 'active' : ''} onClick={() => go(id)} aria-label={label}><span className="nav-icon-wrap"><Icon size={22} strokeWidth={screen === id ? 2.5 : 1.8} />{id === 'cart' && cartCount > 0 && <b className="cart-badge">{cartCount}</b>}</span><small>{label}</small><i /></button>)}</nav>; }
function Filter({ onClose }) { return <div className="modal-backdrop"><div className="filter-modal"><div className="modal-title"><h2>Filter discovery</h2><button onClick={onClose}>×</button></div><p className="saved-note">Browse DOWNTOWN by Pizza, Burger, or Cold Drinks using the category tabs.</p><button className="primary-action full" onClick={onClose}>Done</button></div></div>; }

LegacyProductCardOriginal = ProductCard;
createRoot(document.getElementById('root')).render(<StrictMode><App /></StrictMode>);